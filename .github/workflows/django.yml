name: Django CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  DB_NAME: WalletifyDatabase
  DB_PASSWORD: WalletifyUnsecurePassword
  DB_HOST: localhost
  DB_USER: walletify
  DB_PORT: 8080
  DB_ENGINE: django.db.backends.postgresql
  MYSQL_ATTR_SSL_CA: /etc/ssl/cert.pem

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.9]

    steps:
    - uses: actions/checkout@v3
    - name: Run SQL Server
      run: |
        docker run --name $DB_NAME -e POSTGRES_PASSWORD=$DB_PASSWORD POSTGRES_USER=$DB_USER -p 8080:8080 -d postgres
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install coverage 
        pip install coveralls
    - name: Run Tests
      run: |
        python manage.py test
    - name: Run coverage
      env: 
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        coverage run --source='walletify' manage.py test
        coveralls --service=github
